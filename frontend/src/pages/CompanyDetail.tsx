import React from 'react';
import { Typography } from '@mui/material';

const CompanyDetail: React.FC = () => {
  return <Typography variant="h4">Company Detail Page</Typography>;
};

export default CompanyDetail;